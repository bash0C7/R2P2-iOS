# fetched picoruby tree (kept pristine; rebuilt via rake setup)
/vendor/
# build output
/build/
# staged lib + headers + generated project (per example)
/examples/*/Vendor/
/examples/*/*.xcodeproj
# process docs
/docs/superpowers/
